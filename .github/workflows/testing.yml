name: Daily Workflow

on:
  schedule:
    - cron: '0 0 * * *'  # Chạy lúc 00:00 UTC mỗi ngày

jobs:
  run-daily-task:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run your script
        run: |
          echo "This is a daily task running!"